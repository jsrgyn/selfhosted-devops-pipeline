server {
    listen 80;
    server_name dashboard.local;
    
    root /usr/share/nginx/html;
    index index.html;
    
    location /reports {
        autoindex on;
        alias /usr/share/nginx/html/reports;
    }
}